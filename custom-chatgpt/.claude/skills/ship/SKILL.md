---
name: ship
description: Before shipping, check the current changes, then commit them.
---

1. Run `npm run test`. Abort if any tests fail.
2. Run `npm run lint`. Abort if any linting errors are found.
3. Run `npm run build`. Abort if the build fails.
4. Commit all changes with a generated commit message.
